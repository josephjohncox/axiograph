# Social Examples

`SocialNetwork.axi` is a compact graph-shaped domain example. Use it for simple
relationship queries and relation/path experiments when the family examples are
too semantically loaded.

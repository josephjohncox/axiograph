@prefix ex: <http://example.org/ns#> .
@prefix schema: <http://schema.org/> .
@prefix sh: <http://www.w3.org/ns/shacl#> .
@prefix xsd: <http://www.w3.org/2001/XMLSchema#> .

# A tiny shapes graph: all `schema:Person` must have a string `schema:name` and an integer `schema:age`.

ex:PersonShape
  a sh:NodeShape ;
  sh:targetClass schema:Person ;
  sh:property [
    sh:path schema:name ;
    sh:datatype xsd:string ;
    sh:minCount 1 ;
  ] ;
  sh:property [
    sh:path schema:age ;
    sh:datatype xsd:integer ;
    sh:minCount 1 ;
  ] .


# Semantic Queries

Semantic-query fixtures exercise query/report surfaces without requiring a
full tutorial script. Keep these small and prefer explicit typed scopes over
free-form query text when adding new fixtures.

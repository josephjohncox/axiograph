[package]
name = "axiograph-workspace"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[workspace]
resolver = "2"
members = [
  "crates/axiograph-cli",
  "crates/axiograph-security",
  "crates/axiograph-dsl",
  "crates/axiograph-kernel",
  "crates/axiograph-projections",
  "crates/axiograph-store",
  "crates/axiograph-ingest-sql",
  "crates/axiograph-ingest-docs",
  "crates/axiograph-ingest-json",
  "crates/axiograph-ingest-proto",
  "crates/axiograph-ingest-rdfowl",
  "crates/axiograph-tooling-overlays",
  "crates/axiograph-example-industrial",
  "crates/axiograph-example-regulated-shipment",
  "crates/axiograph-example-software-authoring",
  "crates/axiograph-software-authoring",
  "crates/axiograph-pathdb",
  "crates/axiograph-llm-sync",
  "crates/axiograph-storage",
]

[workspace.package]
version = "20260829.0.3"
edition = "2021"
rust-version = "1.88"
authors = ["Axiograph Team"]
license = "BSD-3-Clause"
repository = "https://github.com/josephjohncox/axiograph"

[workspace.lints.rust]
unsafe_code = "forbid"
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)', 'cfg(verus)'] }

[workspace.dependencies]
# Core utilities
anyhow = "1.0"
thiserror = "2.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ciborium = "0.2"                                   # CBOR serialization
crossbeam-channel = "0.5"
schemars = "1.2"
loom = "0.7.2"

# Parsing
regex = "1.10"
nom = "7.1"

# CLI
clap = { version = "4.6", features = ["derive"] }
colored = "2.1"

# Ingestion
sophia = "0.10" # RDF parsing toolkit (Turtle/N-Triples/N-Quads/TriG)
# oxrdfxml 0.2.3 still requires vulnerable quick-xml 0.37. Pin the maintained
# upstream quick-xml 0.41 migration until the next oxrdfxml release.
oxrdf = { git = "https://github.com/oxigraph/oxigraph.git", rev = "e115a6a8dd9213fdf89a20cb72494ab333878218", features = [
  "rdf-12",
] }
oxrdfxml = { git = "https://github.com/oxigraph/oxigraph.git", rev = "e115a6a8dd9213fdf89a20cb72494ab333878218", features = [
  "rdf-12",
] }
quick-xml = "0.41" # RDF/XML structural preflight and parser dependency
sqlparser = "0.43" # SQL parsing
pdf-extract = "0.12" # PDF text extraction
walkdir = "2.4"

# Async (for gRPC/network)
tokio = { version = "1.52", features = ["full"] }
async-trait = "0.1"
rmcp = { version = "1.5.0", default-features = false, features = [
  "server",
  "macros",
  "schemars",
  "transport-io",
] }
lsp-server = "0.7.9"
lsp-types = "0.97"

# UUID and time
uuid = { version = "1", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
sha2 = "0.10"
tempfile = "3"
process-wrap = { version = "9.1.0", default-features = false }
rustix = { version = "1.1.4", default-features = false }
# 0.40.x uses `cfg_select!` in libsqlite3-sys and does not compile on the pinned Rust 1.88 gate.
rusqlite = { version = "0.39.0", features = [
  "backup",
  "bundled",
  "limits",
  "serialize",
] }

# Logging
tracing = "0.1"
pprof = { version = "0.15", default-features = false, features = [
  "prost-codec",
] }
inferno = { version = "0.12.8", default-features = false }
prost-reflect = "0.16.3"
signal-hook = "0.3"

# PathDB: Efficient binary storage
roaring = { version = "0.10", features = ["serde"] } # Roaring bitmaps
ahash = { version = "0.8", features = ["serde"] }    # Fast hashing
dashmap = "6"                                        # Concurrent hash map
parking_lot = "0.12"                                 # Better mutexes

# HTTP client for LLM APIs
reqwest = { version = "0.12", features = ["json"] }

# Web ingestion / scraping (tooling, untrusted boundary)
url = "2.5"
percent-encoding = "2.3"
scraper = "0.27"
htmd = "0.5.0"
robotstxt = "0.3.0"

# Property testing
proptest = "1.4"

# ============================================================================
# Workspace-level integration tests
# ============================================================================

[[test]]
name = "integration_tests"
path = "tests/integration_tests.rs"

[[test]]
name = "backend_container_tests"
path = "tests/backend_container_tests.rs"

[dev-dependencies]
tempfile.workspace = true
serde_json.workspace = true
axiograph-dsl = { path = "crates/axiograph-dsl" }
axiograph-storage = { path = "crates/axiograph-storage" }
axiograph-pathdb = { path = "crates/axiograph-pathdb" }
axiograph-llm-sync = { path = "crates/axiograph-llm-sync" }
tokio.workspace = true
chrono.workspace = true
reqwest.workspace = true

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
